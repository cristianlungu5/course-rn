import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ListsScreen = props => {
  return (
    <View>
      <Text>Lists Screen Demo</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
  },
});

export default ListsScreen;
